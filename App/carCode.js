module.exports = {
	
	 Car: function () {
    this.model = model;
    this.type = type;
    this.name = name;
    this.numOfDoors = numOfDoors;
    this.numOfwheels = numOfwheels;
    console.log("created");

	 
var Car = (type, model, name, numOfDoors, numOfwheels);
var honda = new Car('Honda');

 if (typeof Car.type === "undefined") {
   console.log({});
 }
  if ( honda instanceof Car ){
  console.log(true);
 }
 
 var gm = new Car();
 if (Car.name === Car()) {

        console.log('General');

      }
	 }
}